# 2026-03-15 工作日志

## MiniRAG 项目完成

- 创建了最小化 RAG 引擎项目，零依赖纯 JavaScript
- 使用 CodeBuddy CLI (`codebuddy -p --output-format json --max-turns 1 --tools ""`) 作为 LLM Provider
- 使用 TF-IDF + bigram 中文分词实现文本向量化（替代 Embedding API）
- 核心文件：`minirag.mjs`（引擎）、`knowledge.mjs`（知识库）、`main.mjs`（演示）
- 已验证运行成功，检索和生成效果良好
- CodeBuddy CLI JSON 输出结构：数组，`type: "result"` 项的 `result` 字段包含回复文本
- Node.js 调用 CodeBuddy CLI 使用 `execFileSync`（同步调用）可以正常获取 stdout
