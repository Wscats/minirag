/**
 * ============================================
 *   示例知识库 - 关于 RAG 技术的文档集合
 * ============================================
 *
 * 这些文档是 MiniRAG 的"知识来源"。
 * RAG 引擎会从中检索与用户问题最相关的段落，
 * 然后让 LLM 基于这些段落生成回答。
 *
 * 实际应用中，这些文档可以来自：
 *   - PDF 文件
 *   - 网页爬取
 *   - 数据库导出
 *   - API 接口
 *   - 用户上传
 */

export const documents = [
  {
    title: "什么是 RAG",
    content: `RAG（Retrieval-Augmented Generation，检索增强生成）是一种将信息检索与大语言模型（LLM）相结合的技术架构。

RAG 的核心思想是：在让 LLM 生成回答之前，先从外部知识库中检索相关文档，然后将检索到的文档作为上下文提供给 LLM，让它基于真实资料来生成回答。

RAG 解决了 LLM 的几个关键问题：
1. 知识过时：LLM 的训练数据有截止日期，无法了解最新信息。RAG 可以接入实时更新的知识库。
2. 幻觉问题：LLM 有时会编造看似合理但实际错误的内容。RAG 让回答有据可依。
3. 领域局限：通用 LLM 可能缺乏特定领域的专业知识。RAG 可以接入专业文档库。

RAG 的典型流程分为两个阶段：
- 索引阶段：将文档分块、向量化，存入向量数据库
- 查询阶段：将问题向量化、检索相似文档、构建提示词、LLM 生成回答`,
  },
  {
    title: "文本向量化技术",
    content: `文本向量化（Text Embedding）是将文本转换为数值向量的过程，是 RAG 系统中最关键的技术之一。

常见的向量化方法有：

1. TF-IDF（词频-逆文档频率）
TF-IDF 是最经典的文本向量化方法。TF（Term Frequency）表示词在当前文档中出现的频率，IDF（Inverse Document Frequency）表示词在所有文档中的稀有程度。一个词在当前文档中出现频繁，但在其他文档中很少出现，说明它对当前文档很重要。TF-IDF 是一种纯统计方法，不需要训练神经网络，计算简单高效。

2. Word2Vec
Word2Vec 是 Google 在 2013 年提出的词向量模型，通过神经网络学习词的分布式表示。它能捕捉词语之间的语义关系，比如 "国王" - "男人" + "女人" ≈ "女王"。

3. Sentence Transformers
基于 BERT 等预训练模型，将整个句子编码为一个稠密向量。这类方法能理解上下文语义，效果最好，但需要 GPU 计算。

4. OpenAI Embedding API
OpenAI 提供了 text-embedding-3-small 等模型，可以通过 API 调用将文本转为 1536 维的向量。使用方便但需要网络调用。`,
  },
  {
    title: "向量数据库",
    content: `向量数据库是专门用于存储和检索高维向量的数据库系统。在 RAG 系统中，向量数据库负责存储文档的向量表示，并支持高效的相似性搜索。

主流的向量数据库有：

1. Pinecone：云原生向量数据库，完全托管，支持实时更新和元数据过滤。
2. Milvus：开源向量数据库，由 Zilliz 开发，支持多种索引类型和混合搜索。
3. Weaviate：开源向量搜索引擎，支持语义搜索和混合搜索。
4. Chroma：轻量级开源向量数据库，专为 AI 应用设计，API 简洁。
5. FAISS：Facebook 开发的向量相似度搜索库，不是完整的数据库，但搜索性能极高。
6. Qdrant：开源向量搜索引擎，支持过滤和负载管理。

向量搜索的核心算法包括：
- 暴力搜索（Brute Force）：遍历所有向量计算相似度，精确但慢，适合小规模数据
- HNSW（分层导航小世界图）：基于图的近似最近邻算法，查询速度快
- IVF（倒排文件索引）：将向量空间分区，只搜索最相关的分区
- PQ（乘积量化）：压缩向量以减少内存使用，适合超大规模数据`,
  },
  {
    title: "分块策略",
    content: `文档分块（Chunking）是 RAG 系统中的重要预处理步骤。分块质量直接影响检索效果和最终回答质量。

常见的分块策略有：

1. 固定大小分块：按字符数或 Token 数切分，简单但可能破坏语义。
2. 段落分块：按自然段落分割，保持语义完整性。
3. 递归分割：先按大分隔符（如章节标题）分割，如果块太大再按小分隔符（如段落、句子）继续分割。
4. 语义分块：使用 NLP 技术判断语义边界，在话题转换处分割。

分块时的关键参数：
- chunk_size（块大小）：通常 200-1000 个字符。太小会丢失上下文，太大会引入噪音。
- chunk_overlap（重叠大小）：相邻块之间的重叠部分，通常是块大小的 10-20%。重叠可以避免在分块边界处丢失重要信息。

分块的最佳实践：
- 先尝试按段落分割，保持语义完整性
- 如果单个段落太长，再按句子分割
- 设置合理的重叠，通常 50-100 字符
- 根据实际场景调整参数，没有万能的设置`,
  },
  {
    title: "RAG 系统的评估与优化",
    content: `评估 RAG 系统的效果需要从检索质量和生成质量两个维度考虑。

检索质量指标：
1. 召回率（Recall）：相关文档被检索到的比例。如果知识库中有 10 个相关段落，检索到了 7 个，召回率就是 70%。
2. 精确率（Precision）：检索结果中相关文档的比例。如果返回了 5 个结果，其中 3 个是相关的，精确率就是 60%。
3. MRR（平均倒数排名）：第一个相关结果的排名位置的倒数的平均值。

生成质量指标：
1. 忠实度（Faithfulness）：回答是否忠实于检索到的文档，没有编造信息。
2. 相关性（Relevance）：回答是否切题，是否回答了用户的问题。
3. 完整性（Completeness）：回答是否覆盖了文档中的所有关键信息。

优化技巧：
- 调整 chunk_size：小块提高精确率，大块提高召回率
- 使用 Reranker：对初步检索结果进行二次排序，提高精确率
- 查询改写：用 LLM 改写用户的模糊问题，提高检索效果
- 混合检索：同时使用向量检索和关键词检索，取长补短
- Hypothetical Document Embedding（HyDE）：让 LLM 先生成一个假设的答案，用假设答案去检索，效果常优于直接用问题检索`,
  },
];
